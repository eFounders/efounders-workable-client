{
  "name": "workable-client-example",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run compile && node .",
    "compile": "babel --presets es2015 -d dist src"
  },
  "dependencies": {
    "@efounders/workable-client": "file:.."
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0"
  }
}
